Django>=5.0,<6.0
django-ninja>=1.0,<2.0
psycopg[binary]>=3.0
gunicorn==21.2.0
uvicorn[standard]==0.27.0
feedparser>=6.0.10
django-redis>=5.4.0
redis>=5.0.0
celery>=5.3.0
django-celery-beat>=2.5.0
requests>=2.31.0
trafilatura>=1.6.0
lxml>=5.1.0
django-cors-headers>=4.3.1
dj-database-url>=2.1.0
# V3 additions
nh3>=0.2.15
structlog>=24.1.0
PyJWT[crypto]>=2.8.0
pytest-django>=4.8.0
pgvector>=0.2.5
fastembed>=0.2.6
# Public Suffix List, for resolving a feed URL to its registrable domain.
# Corroboration counts distinct publishers, so getting this wrong either merges
# rival outlets or lets one newsroom corroborate itself.
tldextract>=5.1.0
ruff>=0.3.0
google-genai>=0.2.0

# Observability. Both optional at runtime: the metrics module degrades to no-ops
# without prometheus-client, and Sentry only initialises when SENTRY_DSN is set.
prometheus-client>=0.20.0
sentry-sdk[django]>=2.0.0
