# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
venv/
.venv/

# Django
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Celery
celerybeat-schedule
celerybeat-schedule.*
celerybeat.pid

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node (frontend has its own .gitignore, but just in case)
node_modules/

# Tool caches (listed here rather than relied on from a global ignore)
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Agent tooling installed locally: skill packs are fetched by
# `npx skills add …` / `npx impeccable install`, not part of the product.
.agents/
.claude/skills/
.claude/settings.local.json
skills-lock.json

# Local design references (screenshots and mood boards, not shipped)
inspo/
